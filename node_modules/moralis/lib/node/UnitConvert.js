"use strict";

var _web = _interopRequireDefault(require("web3"));

var _bignumber = _interopRequireDefault(require("bignumber.js"));

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {
    default: obj
  };
}
/* global window */


const MWeb3 = typeof _web.default === 'function' ? _web.default : window.Web3;

class UnitConverter {
  static ETH(value) {
    return MWeb3.utils.toWei(`${value}`, 'ether');
  }

  static Token(value, decimals) {
    return (0, _bignumber.default)(+value).times(`1e+${decimals}`);
  }

  static FromWei(value, decimals) {
    return (0, _bignumber.default)(+value).div(`1e+${decimals !== null && decimals !== void 0 ? decimals : 18}`).toNumber();
  }

}

module.exports = UnitConverter;