import React from "react";
import { OnAccountChanged } from "../../hooks/core/useMoralis/_useMoralisAuth";
import { Environment, GetMoralis, PluginSpecs } from "../../hooks/core/useMoralis/_useMoralisInit";
interface MoralisProviderOptions {
    onAccountChanged?: OnAccountChanged;
}
export interface MoralisProviderProps {
    children: React.ReactNode;
    appId: string;
    serverUrl: string;
    jsKey?: string;
    dangerouslyUseOfMasterKey?: string;
    plugins?: PluginSpecs[];
    options?: MoralisProviderOptions;
    environment?: Environment;
    getMoralis?: GetMoralis;
}
export declare const MoralisProvider: ({ children, appId, jsKey, dangerouslyUseOfMasterKey, serverUrl, plugins, environment, getMoralis, options: { onAccountChanged }, }: MoralisProviderProps) => JSX.Element;
export {};
